language: php

before_install:
- openssl aes-256-cbc -K $encrypted_2a3b7c31670d_key -iv $encrypted_2a3b7c31670d_iv -in travis.pem.enc -out travis.pem -d

script:
- curl -svo data http://dm4.tw/ip
- chmod 600 ./travis.pem
- ssh -i ./travis.pem git@github.com
